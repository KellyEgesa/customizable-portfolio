import { SkillGroup } from "../models/SkillGroup";

const mobile = new SkillGroup({
  title: "MOBILE APPS",
  statement:
    "Technologies that I use to create cross-platform (IOS and Android) and native mobile applications. I also have experience in deploying applications to the google playstore and appstore. ",
});
